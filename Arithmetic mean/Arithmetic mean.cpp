#include <iostream> // Allows usage of input and output functions such as std::cout.

int main() // Starting point for the program.
{
    std::cout << (15 + 25 + 30 + 20) / 4.0; // Calculates the value of 90 (15 + 25 + 30 + 20) / 4 (22.5) and outputs this to the console without a new line.
}