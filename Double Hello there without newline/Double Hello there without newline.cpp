#include <iostream> // Allows usage of input and output functions such as std::cout.

int main() // Starting point for the program.
{
    std::cout << "Hello there!"; // Outputs "Hello there!" (without the double quotes) to the console without starting a new line.
    std::cout << "Hello there!"; // Outputs the bottom of the hull to the console, and does not start a new line.
}