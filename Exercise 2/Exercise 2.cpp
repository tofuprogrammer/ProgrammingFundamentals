/* Broken code

void printArray(int* arr, int size)
{
    for (int i = 0; i < size; i++)
        std::cout << arr[i] << '\n';
}

int main()
{
    int numbers[] = {3, 4, 5};
    printArray(numbers, sizeof(numbers));
} */