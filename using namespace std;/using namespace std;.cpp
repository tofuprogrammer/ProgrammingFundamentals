#include <iostream> // Allows usage of input and output functions such as std::cout.

using namespace std; // Removes the need for the "std::" prefix by assuming use of the standard library

int main() // Starting point for the program.
{
    cout << "Hello World!\n"; // Outputs "Hello World!" (without the double quotes) to the console and starts a new line.
}