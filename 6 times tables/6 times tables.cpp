#include <iostream> // Allows usage of input and output functions such as std::cout.

int main() // Starting point for the program.
{
    int number = 1; // Defines "number" as a mutable variable of type int (integer) initialised to a value of 1.
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "1 (number) x 6 = 6 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 1 to 2).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "2 (number) x 6 = 12 (number * 6)" to the console followed by a new line. 
    number ++; // Increments number by 1 (changes number from 2 to 3).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "3 (number) x 6 = 18 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 3 to 4).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "4 (number) x 6 = 24 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 4 to 5).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "5 (number) x 6 = 30 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 5 to 6).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "6 (number) x 6 = 36 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 6 to 7).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "7 (number) x 6 = 42 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 7 to 8).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "8 (number) x 6 = 48 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 8 to 9).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "9 (number) x 6 = 54 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 9 to 10).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "10 (number) x 6 = 60 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 10 to 11).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "11 (number) x 6 = 66 (number * 6)" to the console followed by a new line.
    number ++; // Increments number by 1 (changes number from 11 to 12).
    std::cout << number << " x 6 = " << (number * 6) << "\n"; // Outputs "12 (number) x 6 = 72 (number * 6)" to the console followed by a new line.
}