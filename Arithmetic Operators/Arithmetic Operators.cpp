#include <iostream> // Allows usage of input and output functions such as std::cout.

int main() // Starting point for the program.
{
    std::cout << 5 + 5 << "\n"; // Calculates the value of 5 + 5 (10) and outputs this to the console, followed by a new line.
    std::cout << 355.0 / 113.0 << "\n"; // Calculates the value of 355 / 113 (3.14159) and outputs this to the console, followed by a new line.
}